cmake_minimum_required(VERSION 3.7)
set(PROJECT "faf")
include_directories("include")
set(CMAKE_BUILD_TYPE Debug)
file(GLOB sources *.cpp src/*.cpp)
add_executable(${PROJECT} ${sources})
target_link_libraries(${PROJECT} curlpp curl pthread  )
